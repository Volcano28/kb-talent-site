<%= form_for(@test_registration) do |f| %>
  <% if @test_registration.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_registration.errors.count, "error") %> prohibited this test_registration from being saved:</h2>

      <ul>
      <% @test_registration.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :confirmed %><br>
    <%= f.check_box :confirmed %>
  </div>
  <div class="field">
    <%= f.label :confirmation_status %><br>
    <%= f.text_field :confirmation_status %>
  </div>
  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
